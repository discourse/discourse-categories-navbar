<HorizontalOverflowNav>
  {{#each site.categories as |sc|}}
    {{#unless sc.parentCategory}}
      <li>
        {{category-link
          sc
          extraClasses=(if (eq this.currentCategory.slug sc.slug) "active")
        }}
      </li>
    {{/unless}}
  {{/each}}
</HorizontalOverflowNav>